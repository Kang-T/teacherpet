# 함께 만들기 — 참여 안내

티처펫에 관심 가져 주셔서 고맙습니다.
코딩을 전혀 몰라도 참여할 수 있습니다. 할 수 있는 만큼만 해 주세요.

---

## 1. 의견만 남기고 싶어요 (코딩 몰라도 됨)

GitHub 계정만 있으면 됩니다. (계정 만들기: https://github.com/signup — 무료)

1. **아이디어·바라는 점** → [토론 게시판(Discussions)](https://github.com/Kang-T/teacherpet/discussions) → 오른쪽 위 **New discussion**
2. **이상하게 동작해요** → [Issues](https://github.com/Kang-T/teacherpet/issues/new/choose) → **"이상해요"** 양식
3. 가능하면 **화면 캡처**를 붙여 주세요. 글 입력 칸에 그림 파일을 끌어다 놓으면 됩니다.

(아이들 의견은 게임 안의 **"할머니께 편지"** 로 받습니다. 아이들이 GitHub에 글을 쓰지 않게 해 주세요.)

---

## 2. 직접 고쳐서 보내고 싶어요 — fork → 수정 → Pull Request

**용어 세 개만 알면 됩니다.**

- **fork(포크)** — 이 저장소를 내 계정으로 통째로 복사해 오는 것. 내 복사본은 마음대로 고쳐도 원본에 영향이 없습니다.
- **commit(커밋)** — 고친 내용을 "저장"하는 것. 한 줄 설명을 붙입니다.
- **Pull Request(PR, 수정 제안)** — "제가 이렇게 고쳤는데 원본에 넣어 주실래요?" 하고 보내는 것.

### 브라우저만으로 하는 방법 (프로그램 설치 없음)

문구 하나 고치기, 할머니 대사 바꾸기 같은 작은 수정에 알맞습니다.

1. 이 저장소 오른쪽 위 **Fork** 버튼 → **Create fork**
2. 내 복사본에서 고칠 파일을 엽니다. (예: 할머니 대사는 `src/renderer/app.js` 에서 `grannySay` 를 검색)
3. 파일 오른쪽 위 **연필 아이콘(✏️)** → 고칩니다.
4. 오른쪽 위 **Commit changes...** → 무엇을 고쳤는지 한 줄 적고 → **Commit changes**
5. 내 복사본 첫 화면 위쪽에 뜨는 **Contribute → Open pull request** → 설명을 적고 **Create pull request**

끝입니다. 보내 주신 PR은 자동 검사(71개 항목)가 먼저 돌고, 그다음 제가 봅니다.

### 내 컴퓨터에서 실행해 보며 고치는 방법

기능을 추가하거나 여러 파일을 고칠 때 알맞습니다. [README의 개발](README.md#개발) 항목을 보세요.

```bash
npm install
npm run web:serve
```

```bash
npm run check
```

`npm run check` 는 크롬을 화면 없이 띄워 실제 게임을 열고, 닭이 모이를 먹는지·보온등이 작동하는지·새로 시작이 되는지 같은 97가지 행동을 검사합니다. **PR을 보내기 전에 한 번 돌려 주세요.**

---

## 3. 이렇게 처리합니다

- **매주 한 번** 올라온 의견과 PR을 모아 봅니다.
- 받을 것과 받지 않을 것을 골라 업데이트하고, **받지 않은 것에도 이유를 답으로 남깁니다.**
- 우리 반에만 맞는 수정이라 원본에 넣기 어려우면, fork 해서 **선생님 반 버전으로 따로 쓰셔도 됩니다.** (MIT 라이선스라 자유입니다.)

---

## 4. 꼭 지켜 주세요 — 받을 수 없는 수정

아이들이 쓰는 게임이라 아래는 **어떤 이유로도 받지 않습니다.**

1. **개인정보를 묻거나 모으는 것.** 이름·학교·반·얼굴·점수·이메일 모두 개인정보입니다.
2. **인터넷으로 무엇을 보내는 것.** 서버, 통계 도구, 광고, 외부 API 모두 해당합니다. 페이지의 `connect-src 'none'` 설정을 풀지 않습니다.
3. **뽑기(확률형 아이템)·결제·순위표.**
4. **전투·무기·신체를 해치는 표현.** 갈등은 화해·중재로 풉니다.
5. **동물이 고통받는 장면으로 벌주기.** 돌보지 못했을 때도 아이가 "무엇을 하면 되는지" 알 수 있어야 합니다.
6. **브라우저 대화상자**(`confirm` / `prompt` / `alert`). 학교 브라우저에서 막혀 버튼이 먹통이 됩니다. 빌드가 자동으로 막습니다 — 화면 안에서 묻는 `askText()` / `grannySay()` 를 쓰세요.

---

## 5. 코드 짤 때 알아 두면 좋은 것

- 빌드 도구 없는 순수 JavaScript입니다. three.js 는 `src/renderer/` 에 들어 있습니다.
- 닭의 행동 원리는 [docs/설계_닭의_마음.md](docs/설계_닭의_마음.md) 에 있습니다. 행동을 고치기 전에 한 번 읽어 주세요.
- 저장 형식을 바꾸면 **옛 저장을 새 형식으로 옮기는 코드**(마이그레이션)를 함께 넣어 주세요. 아이들의 농장이 사라지면 안 됩니다.
- 새 행동을 만들면 `scripts/checks.probe.js` 에 검사를 하나 더해 주시면 가장 좋습니다.
